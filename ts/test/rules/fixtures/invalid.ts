// const oldFunction = () => { return "removed"; };

// import { deprecated } from 'old-module';

export function activeFunction(): string {
  // let unused = oldFunction();
  return 'active';
}

// if (condition) {
//   doSomething();
// }

// class OldClass {
//   method() { return 1; }
// }

/*
  const removed = {
    key: "value",
    nested: { deep: true },
  };
  removed.process();
*/

// export default function legacyEntry() { return null; }

// await fetch('https://example.com');

// const result = data.map(x => x * 2).filter(x => x > 10);

// try {
//   riskyOperation();
// } catch (e) {
//   console.error(e);
// }

// type OldType = { name: string; value: number };
// interface OldInterface { validate(): boolean; }

// function oldHelper(param: string): boolean { return param.length > 0; }

// throw new Error("never reached");

// for (let i = 0; i < 10; i++) {
//   doWork(i);
// }

/* const multi = {
   line: true,
   removed: "yes"
}; */
