# @thethracian/lint-cli

Patcher CLI for The Thracian lint configs.

```sh
npx @thethracian/lint-cli init rust
npx @thethracian/lint-cli init rust --write
npx @thethracian/lint-cli init elixir --write
```

The executable name is `thx-lint-cli`.
